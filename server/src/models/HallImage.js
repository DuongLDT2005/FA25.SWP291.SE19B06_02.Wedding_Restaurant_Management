class HallImage {
    constructor({ imageID, hallID, imageURL }) {
        this.imageID = imageID;
        this.hallID = hallID;   
        this.imageURL = imageURL;
    }
}

export default HallImage;